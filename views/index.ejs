<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }

        header {
            padding: 15px;
            background-color: #007bff;
            color: white;
            font-size: 24px;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .container {
            margin-top: 50px;
        }

        .card {
            width: 350px;
            margin: auto;
            margin-bottom: 20px;
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
        }

        .card-header {
            background-color: #007bff;
            color: white;
        }
    </style>
</head>

<body>
    <header class="text-center text-light bg-primary d-flex justify-content-between">
        <div>
            <h1>My Book Shop</h1>
        </div>
        <div>
            <a href="/add" class="btn btn-success btn-lg">Add</a>
            <a href="/" class="btn btn-secondary btn-lg">View</a>
        </div>
    </header>
    <div class="container">
        <div class="row row-cols-1 row-cols-md-3 g-4">
            <% record.map((val, i)=> { i = i + 1 %>
                <div class="col">
                    <div class="card h-100">
                        <div>
                            <h5 class="card-title text-center pt-2">
                                <%= i %>. <%= val.name %>
                            </h5>
                        </div>
                        <img src="<%= val.image %>" class="card-img-top p-2" alt="Book Image"
                            style="height: 250px; object-fit: cover;">
                        <div class="card-body">
                            <h5 class="card-title">
                                <%= val.name %>
                            </h5>
                            <p class="card-text">
                                <strong>Price:</strong>
                                <%= val.price %> <br>
                                    <strong>Pages:</strong>
                                    <%= val.pages %> <br>
                                        <strong>Author:</strong>
                                        <%= val.author %>
                            </p>
                        </div>
                        <div class="card-footer text-center">
                            <a href="/deleteRecord?id=<%= val.id %>" class="btn btn-danger me-2">Delete</a>
                            <a href="/editRecord?id=<%= val.id %>" class="btn btn-primary">Edit</a>
                        </div>
                    </div>
                </div>
                <% }) %>
        </div>
    </div>
</body>

</html>