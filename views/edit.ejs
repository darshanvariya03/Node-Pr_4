<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Document</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
    }
    header {
            padding: 15px;
            background-color: #007bff;
            color: white;
            font-size: 24px;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

    .container {
      margin-top: 50px;
    }

    .card {
      width: 500px;
      margin: auto;
      margin-bottom: 20px;
      box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
    }

    .card-header {
      background-color: #007bff;
      color: white;
    }
  </style>
</head>
<body>
    <header class="text-center text-light bg-primary d-flex justify-content-between">
        <div>
            <h1>My Book Shop</h1>
        </div>
        <div>
            <a href="/add" class="btn btn-success btn-lg">Add</a>
            <a href="/" class="btn btn-secondary btn-lg">View</a>
        </div>
    </header>
<div class="container mt-5">
  <div class="card">
    <div class="card-header">
      <h1 class="text-center">Edit Your Book</h1>
    </div>
    <div class="card-body">
      <form method="post" action="/updateRecord" enctype="multipart/form-data">
        <input type="hidden" name="editid" value="<%= single.id %>">
        <div class="form-group">
          <label for="name">Name:</label>
          <input type="text" class="form-control" id="name" name="name" value="<%= single.name %>">
        </div>
        <div class="form-group">
          <label for="price">Price:</label>
          <input type="text" class="form-control" id="price" name="price" value="<%= single.price %>">
        </div>
        <div class="form-group">
          <label for="pages">Pages:</label>
          <div class="form-check d-flex">
            <input class="form-check-input" type="radio" <% if(single.pages.includes('10')) { %> checked <% } %> name="pages" value="10">0-10
          </div>
          <div class="form-check d-flex">
            <input class="form-check-input" type="radio" <% if(single.pages.includes('25')) { %> checked <% } %> name="pages" value="25">10-25
          </div>
          <div class="form-check d-flex">
            <input class="form-check-input" type="radio" <% if(single.pages.includes('50')) { %> checked <% } %> name="pages" value="50">25-50
          </div>
          <div class="form-check d-flex">
            <input class="form-check-input" type="radio" <% if(single.pages.includes('100')) { %> checked <% } %> name="pages" value="100">100
          </div>
        </div>
        <div class="form-group">
          <label for="author">Author:</label>
          <input type="text" class="form-control" id="author" name="author" value="<%= single.author %>">
        </div>
        <div class="form-group">
          <label for="avtar">File Upload:</label>
          <div class="custom-file">
            <input type="file" class="custom-file-input" id="avtar" name="avtar">
            <label class="custom-file-label" for="avtar">Choose file</label>
          </div>
          <img src="<%= single.image %>" width="150" alt="Book Image">
        </div>
        <button type="submit" value="submit" class="btn btn-primary">Submit</button>
      </form>
    </div>
    <div class="card-footer text-muted text-center">
        <a href="/" class="btn btn-secondary">View</a>
    </div>
  </div>
</div>

</body>
</html>
